platform :ios, '10.0'
use_frameworks!

target 'Colander_Example' do
  pod 'Colander', :path => '../'

  target 'Colander_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 4.0.0'
    pod 'Nimble', '~> 9.2.0'
  end
end
