platform :ios, '10.0'
use_frameworks!

target 'Colander_Example' do
  pod 'Colander', :path => '../'

  target 'Colander_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 2.1.0'
    pod 'Nimble', '~> 8.0.1'
  end
end
